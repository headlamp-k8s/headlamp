{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "args": ["-dev", "-enable-dynamic-clusters"],
          "name": "headlmap-server",
          "sidecar": true
        }
      ]
    },
    "shell:allow-open"
  ]
}
